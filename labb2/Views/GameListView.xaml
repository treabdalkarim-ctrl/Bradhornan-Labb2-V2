<Window x:Class="labb2.Views.GameListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Spelbibliotek" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <ListBox ItemsSource="{Binding Games}"
                 SelectedItem="{Binding SelectedGame}"
                 DisplayMemberPath="Title"
                 Grid.Column="0"/>

        <StackPanel Grid.Column="1" Margin="20">
            <TextBlock Text="{Binding SelectedGame.Title}" FontSize="20" Margin="0 0 0 10"/>
            <TextBlock Text="{Binding SelectedGame.Category}" Margin="0 0 0 10"/>

            <Button Content="Ladda spel"
                    Command="{Binding LoadGamesCommand}" Margin="0 0 0 10"/>

            <Button Content="Lägg till spel"
                    Command="{Binding AddGameCommand}" Margin="0 0 0 10"/>

            <Button Content="Ta bort valt spel"
                    Command="{Binding DeleteGameCommand}" Margin="0 0 0 10"/>
        </StackPanel>
    </Grid>
</Window>
